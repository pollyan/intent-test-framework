# Langfuse 配置说明
# 在本地 .env 文件中添加以下配置

# Langfuse 服务密钥（生成的随机值）
LANGFUSE_SECRET=dzc4VPLXUiqD-4PfOGwIrRAzDN7lUfzZSPZX2HHsgXk
LANGFUSE_SALT=Tk530kI7uJnV8OhNTEVxkiwgcKX3g7KLGbj2YZyShvo

# Langfuse API 密钥（首次访问 Web UI 后生成并填入）
LANGFUSE_PUBLIC_KEY=pk-lf-915ea67b-477d-46c1-8b5c-32dd966c61d3
LANGFUSE_SECRET_KEY=sk-lf-8c471a78-b297-4a4b-a741-f6e4adf7aa61
LANGFUSE_HOST=http://localhost:3000

# 说明：
# 1. LANGFUSE_SECRET 和 LANGFUSE_SALT 用于 Langfuse 服务器内部加密
# 2. LANGFUSE_PUBLIC_KEY 和 LANGFUSE_SECRET_KEY 需要在首次访问 http://localhost:3000 后：
#    - 创建管理员账户
#    - 登录后进入 Settings → API Keys
#    - 创建新的 API Key 
#    - 将生成的 Public Key 和 Secret Key 填入上面的环境变量
# 3. 填入后重启 web-app 容器以加载新配置
